badge($bg-color=$color-p)
    ui-font()
    bg-color($bg-color)
    display: inline-flex
    justify-content: center
    align-items: center
    font-size: 0.7em
    border-radius: 20em
    padding: 0.2em
    min-width: 1.6em
    min-height: 1.6em
